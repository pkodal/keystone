<?php

include_once(realpath(dirname(__FILE__) . '/config/config.inc'));
include_once(realpath(dirname(__FILE__) . '/framework/bootstrap.inc'));
include_once(realpath(dirname(__FILE__) . '/../app/bootstrap.inc'));
include_once(realpath(dirname(__FILE__) . '/orm/bootstrap.inc'));

?>
